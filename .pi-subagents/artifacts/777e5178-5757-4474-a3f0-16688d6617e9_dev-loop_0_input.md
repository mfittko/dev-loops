# Task for dev-loop

Resume dev-loop follow-up for issue #1035 / PR #1079, repo mfittko/dev-loops.

Inputs (deterministic): issue=1035, pr=1079, repo=mfittko/dev-loops. Last-known loop state: round_cap_clean_fallback (CI success, 0 unresolved threads).

Procedure:
1. Resolve dev-loops package root via the bounded candidates in the dev-loop skill (node module resolution → ~/.pi/agent/npm/node_modules/dev-loops → package-relative → global). Never unbounded find.
2. Startup: `node <pkg>/cli/index.mjs loop startup --issue 1035` then `loop build-envelope --input <resolver-output.json>`. Load only requiredReads from the envelope.
3. Pre-flight PR gate (mandatory): `node scripts/loop/copilot-pr-handoff.mjs --repo mfittko/dev-loops --pr 1079`. Abort if action:stop. Proceed inline if terminal:true; resolve the blocking condition first if terminal:false.
4. Worktree cwd (mandatory): fetch origin first, then use a worktree checkout for any git/file/validation ops — never the main checkout.
5. Route: external_pr_followup → load copilot-pr-followup skill + copilot-loop-operations doc + (if final_approval reached) final-approval skill. Execute envelope nextAction.
6. Round-cap budget (default 5): do NOT re-request Copilot review past the cap. round_cap_clean_fallback means rounds are capped with clean threads — proceed toward gate/merge-readiness, do not re-request Copilot.
7. Post gate evidence (draft_gate and current-head pre_approval_gate) via the full upsert-checkpoint-verdict.mjs helper, or the gh-only fallback poster if the helper is unreachable.
8. STOP RULE: stop at the human approval checkpoint. Do NOT merge, do NOT mark ready-for-review, do NOT enable auto-merge without explicit human authorization. This run is follow-up + gate evidence only.
9. Report back: final loop state, gate verdict(s), blocking-severity findings if any, CI status, and the explicit next action requiring a human decision.

---
**Output:**
Write your findings to exactly this path: /Users/mfittko/github/dev-loops/.pi-subagents/artifacts/outputs/777e5178-5757-4474-a3f0-16688d6617e9/tmp/dev-loop-runs/pr-1079.out.md
This path is authoritative for this run.
Ignore any other output filename or output path mentioned elsewhere, including output destinations in the base agent prompt, system prompt, or task instructions.

## Acceptance Contract
Acceptance level: attested
Completion is not accepted from prose alone. End with a structured acceptance report.

Criteria:
- criterion-1: Return a concise result and residual risks when applicable

Required evidence: manual-notes, residual-risks

Finish with a fenced JSON block tagged `acceptance-report` in this shape:
Use empty arrays when no items apply; array fields contain strings unless object entries are shown.
```acceptance-report
{
  "criteriaSatisfied": [
    {
      "id": "criterion-1",
      "status": "satisfied",
      "evidence": "specific proof"
    }
  ],
  "changedFiles": [
    "src/file.ts"
  ],
  "testsAddedOrUpdated": [
    "test/file.test.ts"
  ],
  "commandsRun": [
    {
      "command": "command",
      "result": "passed",
      "summary": "short result"
    }
  ],
  "validationOutput": [
    "validation output or concise summary"
  ],
  "residualRisks": [
    "none"
  ],
  "noStagedFiles": true,
  "diffSummary": "short description of the diff",
  "reviewFindings": [
    "blocker: file.ts:12 - issue found, or no blockers"
  ],
  "manualNotes": "anything else the parent should know"
}
```
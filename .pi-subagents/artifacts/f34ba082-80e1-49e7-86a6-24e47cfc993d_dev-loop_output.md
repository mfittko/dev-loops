Final state confirmed: commit `08b8fc07` contains exactly the 2 intended files, index is clean (noStagedFiles=true), HEAD matches `origin/issue-1087`, main checkout untouched. The `node_modules` symlink is a worktree-local untracked test artifact (not staged/committed).

## Status Summary

**PR #1107** (draft, self-assigned, `Closes #1087`) — branch `issue-1087`, head SHA `08b8fc07`, base `origin/main` `6d073174`.

**Implementation done & validated.** Restored `--timeout-ms` as a recognized flag in `probe-copilot-review.mjs` (mirrors the working sibling `probe-ci-status.mjs`): `--timeout-ms 0` now performs a single immediate check returning `idle` (the underlying `watchCopilotReview` already supported `timeoutMs===0`; only `REMOVED_FLAGS` blocked it). This also fixes the silent strip-to-watch: `cli/index.mjs`'s `buildCorrectedArgs` no longer strips `--timeout-ms` on a usage error → no silent 30-min watch. Documented the heartbeat/stderr liveness contract + `capture-threads` pointer in USAGE. Tests 19/19 pass.

**BLOCKED at gate fanout (tooling limitation, not a logic failure).** `draft_gate` resolves to `full_fanout` (47 lines > 20-line `lightMode` ceiling). The `draft_gate`/`pre_approval_gate` fanout_fanin requires dispatching fresh-context review subagents, but the `subagent` tool is genuinely not available in my runtime (callable set: `bash`/`intercom`/`contact_supervisor` only). Verified: pi-subagents extension not installed; `pi-subagents` CLI is only an installer; and per pi's own design the subagent tool is parent-only — delegated child subagents don't receive it. `requireFanoutEvidence: true` forbids faking the fanout. I escalated to the supervisor; their standing instruction for this case was "report that exact fact and stop — do not fall back to inline/fabricated findings." Intercom ask then timed out (10 min, no reply); I registered the blocker via a non-blocking progress_update. **Awaiting decision:** (A) parent orchestrates the fanout then re-dispatches me for the script-driven post-gate steps (request-copilot-review → run-watch-cycle → fixer → merge), (B) enable the pi-subagents extension in my session, or (C) other.

The 2 local `test/contracts/claude-hooks-settings.test.mjs` failures are pre-existing/environmental (fail identically on clean `origin/main` with real `node_modules`; main CI is green) and unrelated to this PR.
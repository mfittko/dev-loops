# Task for dev-loop

User instruction: continue the dev-loop queue and ship the next patch version for this repository.

Operate as the dev-loop async worker, not the main agent. Follow the installed dev-loop skill and repo AGENTS.md exactly:
- Resolve authoritative queue/current state through dev-loop startup/resolver/envelope; do not rely on chat context.
- Use only tracked artifact origins (issue, persisted plan, or sanctioned lightweight PR-body spec).
- Always use a worktree for git/file/validation work; fetch origin before creating/reusing it.
- Continue one bounded item at a time according to queue/canonical state.
- Prefer the GitHub-first routed path and canonical dev-loops wrappers; never raw gh reads where wrappers exist.
- Run required gates/verification and post required checkpoint evidence.
- User authorized continuing queue and preparing/shipping the next patch version, but do NOT perform any force-push, branch deletion/recreation, reset moving branch heads, rebase-to-push, or merge that changes remote branch state unless the current conversation contains explicit target-specific approval. Stop at any human merge/release approval gate and report exact needed approval.
- Stop and report if state is ambiguous, retrospective/reconcile blocks, or no queue item is actionable.

Return: active artifact(s), branch/PR/version, validation/gate evidence, what shipped or what approval is still needed.

## Acceptance Contract
Acceptance level: reviewed
Completion is not accepted from prose alone. End with a structured acceptance report.

Criteria:
- criterion-1: Implement the requested change without widening scope
- criterion-2: Return evidence sufficient for an independent acceptance review

Required evidence: changed-files, tests-added, commands-run, validation-output, residual-risks, no-staged-files

Review gate: required by reviewer.

Finish with a fenced JSON block tagged `acceptance-report` in this shape:
Use empty arrays when no items apply; array fields contain strings unless object entries are shown.
```acceptance-report
{
  "criteriaSatisfied": [
    {
      "id": "criterion-1",
      "status": "satisfied",
      "evidence": "specific proof"
    }
  ],
  "changedFiles": [
    "src/file.ts"
  ],
  "testsAddedOrUpdated": [
    "test/file.test.ts"
  ],
  "commandsRun": [
    {
      "command": "command",
      "result": "passed",
      "summary": "short result"
    }
  ],
  "validationOutput": [
    "validation output or concise summary"
  ],
  "residualRisks": [
    "none"
  ],
  "noStagedFiles": true,
  "diffSummary": "short description of the diff",
  "reviewFindings": [
    "blocker: file.ts:12 - issue found, or no blockers"
  ],
  "manualNotes": "anything else the parent should know"
}
```
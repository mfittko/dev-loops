- **The judge verdict validator now shares one `scopeDrift.verdict` vocabulary with the judge persona and normalizes the intuitive `none` spelling (issue [#2260](https://github.com/mfittko/dev-loops/issues/2260)).** The accepted set (`within_scope`, `drift_detected`) is defined once as `SCOPE_DRIFT_VERDICTS` in `@dev-loops/core/loop/gate-fanin` and consumed by the validator; a divergence-guard test binds the judge persona's documented set to it so producer and enforcer cannot silently drift apart. A persona-following judge that records `scopeDrift.verdict: "none"` for the no-drift case now passes: the validator normalizes `none` to `within_scope` before any reject path, disposition and rationale byte-intact, so a semantically-correct verdict no longer wastes a resend round.

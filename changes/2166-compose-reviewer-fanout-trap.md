### Fixed

- **`scripts/github/compose-reviewer-prompt.mjs`'s CLI no longer dispatches a fan-out round that fails closed only at fan-in — it now refuses and names `scripts/github/emit-fanout-dispatch.mjs` as the one sanctioned path (issue [#2166](https://github.com/mfittko/dev-loops/issues/2166)).** The composer CLI built ONE reviewer prompt per invocation but never wrote the keyed `<gate>-<headSha>.emit-plan.json` fan-in requires (`GATE-EXEC-FANOUT-DISPATCH-EMIT`, issue #2152/#2092), so a conductor who followed the docs and ran it per-unit produced reviewer prompts with no emit-plan, and the round failed only at `consolidate-fanin` — a latent trap docs still pointed conductors at. The CLI now refuses every direct fan-out invocation (still serves `--help`) and names `emit-fanout-dispatch.mjs` — which drives the composer's exported core (`composeAndRecordReviewerPrompt`) internally for every dispatch unit AND persists the emit-plan — as the sanctioned whole-round dispatch path. The exported `composeAndRecordReviewerPrompt` core (and `composeReviewerPromptText`) are unchanged: they remain the legitimate internal building block `emit-fanout-dispatch.mjs` calls. `skills/docs/gate-review-sub-loop-contract.md`, `skills/copilot-pr-followup/SKILL.md`, and `agents/review.agent.md` no longer instruct a conductor to run the composer CLI directly for fan-out; they now attribute per-unit prompt composition to the emitter (which drives the composer core internally). Regression-pinned in `test/github/compose-reviewer-prompt.test.mjs` (the trap is closed: the CLI refuses even valid fan-out args and names `emit-fanout-dispatch.mjs`; composition coverage moved to direct calls of the exported core) and `test/github/emit-fanout-dispatch.test.mjs` (unchanged: the emitter writes the keyed emit-plan on success and leaves none on refusal). `.claude/` mirror regenerated in lockstep.

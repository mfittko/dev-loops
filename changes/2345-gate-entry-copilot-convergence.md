### Fixed

- **The pre_approval gate-entry coordinator now recognizes the same below-cap "settled" Copilot convergence states the merge gate already treats as converged (issue [#2345](https://github.com/mfittko/dev-loops/issues/2345)).** Both now route through one shared `evaluateCopilotConvergence`. A thread-clean current-head 🔵 "Needs a closer look" (0 unresolved threads) no longer forbids pre_approval entry; unresolved threads and a 🟡 "Changes recommended" body still block it. A proven below-cap integrate-only base-move carry-forward is recognized as settled at gate entry too, re-verified live and fail-closed, via the shared `classifyPrOwnDeltaSinceLastReview` reduction. `evaluateCopilotConvergence` is now review-shape-tolerant (REST and GraphQL shapes), so the entry detector and the merge gate always agree on one verdict.
